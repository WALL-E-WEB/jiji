## Worker

```
onInit中使用;

ever(litnersval,(val){});
once(litnersval,(val){});

/// 防抖 等待3秒后触发
debounce(litnersval,(val){},time:Duration(seconds:3));
/// 节流 3秒只触发一次
interval(litnersval,(val){},time:Duration(seconds:3));
```

